export enum ColumnsType {
    PrimaryKey,
    PrimaryAutoGeneratedKey,
    ManualIncrement,
    Data,
    Relation
}

export interface ColumnInfo {
    name: string,
    type?: ColumnsType,
    length?: number
}